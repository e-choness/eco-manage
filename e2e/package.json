{
  "name": "ecomanage-e2e",
  "version": "2.0.0",
  "private": true,
  "description": "End-to-end smoke tests, run against the docker-compose stack",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.49.0",
    "@types/node": "^20.11.5"
  }
}
