{
  "name": "@ecomanage/shared",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Types, zod schemas, MQTT topics, units, sign rules and site-time helpers shared by every app",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run --coverage"
  },
  "license": "ISC",
  "dependencies": {
    "luxon": "^3.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "@vitest/coverage-v8": "^1.6.1",
    "eslint": "^9.13.0",
    "typescript": "~5.6.2",
    "vitest": "^1.6.1"
  }
}
