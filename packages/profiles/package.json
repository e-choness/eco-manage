{
  "name": "@ecomanage/profiles",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Device profiles (register maps, states, faults, write limits, fixes) and their loader",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "license": "ISC",
  "dependencies": {
    "@ecomanage/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "typescript": "~5.6.2",
    "vitest": "^1.6.1"
  }
}
