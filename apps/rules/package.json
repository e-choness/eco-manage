{
  "name": "@ecomanage/rules",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Rules service: alert checks on every reading (P2-07); recommendations follow in Phase 3",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "license": "ISC",
  "dependencies": {
    "@ecomanage/db": "workspace:*",
    "@ecomanage/shared": "workspace:*",
    "ioredis": "^5.4.1",
    "mongoose": "^8.1.1",
    "pino": "^9.5.0",
    "tsx": "^4.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@ecomanage/simulator": "workspace:*",
    "@types/node": "^20.11.5",
    "eslint": "^9.13.0",
    "typescript": "~5.6.2",
    "vitest": "^1.6.1"
  }
}
